YAGL

-Window: glfw
-Graphics: OpenGL
-Math: glm

Graphics:
    Model:
        -vector<ref<Mesh>>
        -vetor<vector<Material>
        -vector<vector<ref<Animation>>
    
    Mesh:
        -vector<Vertex>
        -ref<VertexBuffer>

    Animation:
        -vector<Bone>

    Material:
        -Color
        -Textures
    
    Program
        -VertexShader
        -FragmentShader
        -vector<uniforms>
            positition
            normal
            uv

    RenderPass:
        -vector<Ref<Model>>
        -RenderTarget
        -Shader
        -

